import 'package:flutter/material.dart';
import 'data.dart';

class Rediger extends StatelessWidget {
  static const routeName = "/rediger";

  @override
  Widget build(BuildContext context) {
    

    return Scaffold(

    );
  }
}